# =============================================================================
# Anthropic Configuration (Claude Models)
# =============================================================================
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# =============================================================================
# Claude Agent SDK Configuration
# =============================================================================
# Path to Claude Code CLI (optional, defaults to PATH lookup)
# CLAUDE_CODE_CLI_PATH=/usr/local/bin/claude

# Permission mode for SDK operations (optional, defaults to "default")
# Options: "default", "plan", "acceptEdits", "bypassPermissions"
CLAUDE_SDK_PERMISSION_MODE=default

# Maximum tokens for SDK requests (optional, defaults to 8000)
CLAUDE_SDK_MAX_TOKENS=8000

# Working directory for SDK operations (optional, defaults to project root)
# CLAUDE_SDK_WORKING_DIR=/Users/vics/Applications/agent-ironman-app

# =============================================================================
# Z.AI Configuration (GLM Models)
# =============================================================================
# Get your API key from: https://z.ai
# The server automatically configures the endpoint when you select a GLM model
ZAI_API_KEY=your-zai-api-key-here

# =============================================================================
# Redis Configuration (Task Queue)
# =============================================================================
# Required for Python worker integration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# =============================================================================
# LLM Configuration
# =============================================================================
# Default model to use (optional, defaults to claude-3-5-sonnet-20241022)
DEFAULT_MODEL=claude-3-5-sonnet-20241022

# Maximum tokens per request (optional, defaults to 8000)
MAX_TOKENS=8000

# Temperature for responses (optional, defaults to 0.7)
TEMPERATURE=0.7

# Maximum document upload size in MB (optional, defaults to 50)
MAX_DOCUMENT_SIZE_MB=50

# =============================================================================
# Vector Store Configuration
# =============================================================================
# Directory for ChromaDB persistent storage (optional, defaults to ./data/chroma)
CHROMA_PERSIST_DIR=./data/chroma

# =============================================================================
# Server Configuration
# =============================================================================
# Port the server runs on (optional, defaults to 3003)
PORT=3003
